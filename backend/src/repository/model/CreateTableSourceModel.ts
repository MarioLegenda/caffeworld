import TableDate from "./TableDate";

export default class CreateTable {
    nickname: string;
    subject?: string;
    subjectDescription?: string;
    datetime?: TableDate;
    briefMessage?: string;
    createdAt?: string;
}
